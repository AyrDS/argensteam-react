const ItemDetail = ({ gifs }) => {
    console.log(gifs)
    return (
        <>


            <div>
                {/* <img src={url} alt={title} />
                <h1>{title}</h1> */}
            </div>


        </>
    );
}

export default ItemDetail;